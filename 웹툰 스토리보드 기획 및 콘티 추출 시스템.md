1. 프로젝트 개요
소설이나 시나리오 텍스트를 입력하면 AI가 이를 분석하여 웹툰 제작을 위한 상세 스토리보드(콘티)를 추출해주는 웹 애플리케이션입니다. 이미지 생성 기능은 포함하지 않으며, 전문적인 이미지 생성 프롬프트와 연출 가이드를 텍스트로 제공하는 것에 집중합니다.
2. 핵심 기능 요구사항
A. 소설 입력 및 분석 (Editor)
텍스트 입력: 대용량 소설 텍스트를 붙여넣을 수 있는 텍스트 영역.
스토리보드 생성: 버튼 클릭 시 AI(Gemini API 등)를 호출하여 텍스트를 분석하고 구조화된 JSON 데이터로 변환.
결과 표시: 추출된 패널별로 [장면 설명, 이미지 프롬프트, 나레이션, 대사]를 카드 형태로 시각화.
B. 시리즈 바이블 (Series Bible) - 설정 관리
캐릭터(Characters): 이름, 외형 묘사, LoRA 트리거 단어 관리.
장소(Locations): 배경 명칭 및 상세 묘사 관리.
아이템/소품(Objects): 주요 소품이나 탈것의 특징 관리.
영속성: 입력된 설정은 브라우저(LocalStorage)에 저장되어 다음 작업 시에도 유지됨.
C. 제작 가이드 및 제약 사항 (Style & Constraints)
스타일 가이드: 지향하는 화풍(예: 현대 판타지, 로맨스 판타지 등) 설정.
전역 제약 사항: 모든 패널에서 지켜야 할 규칙(예: 우측통행, 특정 로고 제외 등) 설정.
화면 비율: 9:16(세로), 3:4(표준), 16:9(가로) 등 콘티의 기본 비율 설정.
D. 데이터 관리 (Data Management)
가져오기/내보내기: 작업 중인 스토리보드와 시리즈 바이블 설정을 텍스트 파일로 소설 내용과 프롬프트를 저장하고 불러오기.
전체 초기화: 모든 데이터를 삭제하고 초기 상태로 되돌리는 기능.
3. 기술 스택 및 UI/UX 가이드라인
Framework: React (Vite), TypeScript.
Styling: Tailwind CSS (Modern, Clean, Minimalist 디자인).
Icons: Lucide React.
Animation: Framer Motion (부드러운 탭 전환 및 모달 효과).
UI 구성:
상단: 앱 이름 및 전체 초기화/저장하기 불러오기 설정 버튼이 있는 헤더.
중앙: '에디터(입력)'와 '시리즈 바이블(설정)'을 오가는 탭 시스템.
하단: 작업 진행 상태 및 도움말 정보.
4. AI 프롬프트 로직 (핵심 엔진)
AI에게 전달할 프롬프트는 다음 구조를 따라야 합니다:
역할 정의: "당신은 전문 웹툰 스토리보드 작가입니다."
입력 데이터: [소설 텍스트] + [시리즈 바이블 설정] + [스타일 가이드].
출력 형식: 반드시 다음 필드를 포함하는 JSON 객체여야 함.
title: 스토리 제목
characters: 분석된 캐릭터 리스트
panels: 패널 번호, 장면 묘사, 이미지 생성용 상세 프롬프트(영어), 나레이션, 대사.
특이사항: 입력값이 '슬라이드 목차' 형태일 경우 해당 구조를 엄격히 준수할 것.

---

## 5. [시스템 고도화 및 주요 업데이트 내역 - 최근 작업 기준]

### A. ComfyUI 연동을 통한 인앱(In-App) 이미지 생성
*   초기 기획과 달리, 프론트엔드에서 직접 **ComfyUI(http://localhost:8188)** API와 통신하여 이미지를 생성하고 화면에 띄우는 기능을 추가 구현함.
*   `comfyUIService.ts`를 통해 `Z-Anime-Workflow-v1.json`과 완벽히 동기화된 워크플로우 페이로드를 구성하여 전송.
*   **동적 설정 연동:** 앱 내 설정창에서 **생성 모델(UNET Model)** 및 **업스케일(Upscale) 사용 여부**를 사용자가 직접 변경하면 워크플로우에 즉시 반영되도록 구현 (불필요한 연산 방지).
*   **보안 이슈 해결:** ComfyUI 실행 시 `--enable-cors-header "*"` 옵션을 추가하여 CORS(403) 차단 문제를 우회하도록 지침 확립.

### B. 로컬 파일 시스템 직접 연동 (File System Access API)
*   웹 브라우저의 한계를 넘어, 내 컴퓨터(C드라이브 등)의 특정 폴더를 지정하여 생성된 파일들을 다이렉트로 저장/관리하는 `fileSystemService.ts` 구축.
*   **images 폴더 (작업장):** 패널별로 생성 버튼을 누를 때마다 `panel_01_고유ID.png` 형태로 덮어쓰기 없이 모든 생성 히스토리를 누적 저장.
*   **exports 폴더 (최종 납품용):** [저장 / 추출] 버튼 클릭 시, 앱 화면에서 최종 선택된(Active) 이미지만 모아서 번호에 맞게 `panel_01.png` 및 `panel_01.txt` 세트로 일괄 복사/추출.

### C. 덮어쓰기(Overwrite) 완벽 방지 설계
*   텍스트 원문을 갈아끼우며 여러 화(Episode)를 연속으로 추출할 때, 기존 추출본이 날아가는 사고를 방지하기 위해 **타임스탬프 기반 독립 폴더 생성 로직** 적용.
*   추출 시 `exports/콘티추출_2026-05-10T22-04-01/` 형식의 하위 폴더가 자동 생성되어 각 회차별 파일이 서로 섞이거나 덮어씌워지지 않음.
*   **상태 동기화 원칙:** 앱의 히스토리에서 휴지통(삭제) 아이콘으로 지워버린 이미지는 로컬 폴더에 원본이 남아있더라도 `exports` 폴더로는 추출되지 않음 (작업자의 최종 Pick만 깔끔하게 내보냄).

### D. 로컬 AI (Ollama) 안정화 및 JSON 파싱 강화
*   제미나이(Gemini) 외에도 로컬 모델(Qwen 등)을 통한 텍스트 콘티 추출을 지원함.
*   **토큰 한계 극복:** 로컬 모델이 긴 원문을 읽고 JSON을 뱉다가 끊기는(`Unexpected end of JSON input`) 현상을 방지하기 위해 `num_ctx`와 `num_predict`를 각각 8192 토큰으로 대폭 상향 적용.
*   **파싱 정제 로직:** 모델이 앞뒤로 덧붙이는 불필요한 대화문("Here is your JSON:")을 무시하고, 첫 `{` 와 마지막 `}` 사이의 데이터만 정확히 파싱해내는 강제 추출 로직을 도입하여 안정성 확보.
